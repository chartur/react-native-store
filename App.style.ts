import {StyleSheet} from 'react-native';
import {APP_BG_COLOR} from './theme';

const styleSheet = StyleSheet.create({
  scaffold: {
    height: '100%',
    backgroundColor: APP_BG_COLOR,
    paddingHorizontal: 16,
    paddingTop: 10,
  },
});

export default styleSheet;
